# 2026-08-23

## Research Skill Layer Phase A

- 将外部Research Skill方案裁决为可执行版本并写入`04-分析/01-current/Research Skill Layer可执行实施方案.md`；
- 明确保留现有P1–P4规则，不修改P0/P1门槛；
- 新增统一Skill Contract、Registry、Execution Context、Result、缓存键、脱敏和JSON Trace；
- 新增P1检索、P2确定性聚类、P3证据空白、P4受控开题薄Adapter；
- 锁定6个外部项目当前审计commit，全部保持disabled、deferred或reference_only；
- 新增10项Research Skill Layer测试；
- 知识库P1–P4与Skill Layer测试65项通过，Demo回归26项通过。

## Research Skill Layer Phase B

- 新增61方向通用的五视角确定性SearchPlan；
- 新增OpenAlex前向/后向引文扩展，按稳定标识去重并计算seed degree；
- 所有引文邻居只作为Discovery候选，统一要求重新进入P1；
- 新增Crossref/OpenAlex引用元数据和撤稿状态核验；
- 新增建议性检索饱和度，不修改P0/P1门槛；
- 新增13项Discovery Loop测试；知识库全量78项、Demo回归26项通过。

## Research Skill Layer Phase C

- 新增四角色Gap Falsification确定性查询计划；
- 反证发现必须重新经过既有P1并为direct；
- 正式反证必须绑定摘要/全文Evidence Span，题名级、adjacent、撤稿和错配记录被拒绝；
- 直接解决、矛盾、边界和替代解释分别产生降级、重查或收窄建议；
- PARTIAL搜索不得确认空白存活，系统不自动改写原Gap；
- 新增9项Gap Falsification测试；知识库全量87项、Demo回归26项通过。

## Research Skill Layer Phase D-1

- 新增默认关闭的GROBID全文证据Provider和P3 Skill Adapter；
- 实现健康/版本检查、PDF类型/大小/签名校验和受控multipart全文请求；
- 将TEI句子转换为带章节、页码和坐标的fulltext Evidence Span；
- 全文Evidence Matrix可直接通过既有Claim Store绑定校验；
- Provider不复制PDF、不持久化原始TEI，也不在Skill结果中输出本地PDF路径；
- 未配置、服务不可用、空结果或解析失败均保留明确阻断/失败状态，不使用题名降级；
- 当前开发机未安装Docker，因此只完成客户端和离线解析验收，未声称GROBID容器已部署；
- 新增10项全文证据测试；知识库全量97项、Demo回归26项通过。

### D-1真实运行时验收

- 创建者下载Docker后，确认Docker Desktop已按用户安装；补装WSL2并成功启动Docker Engine 29.7.2；
- 拉取并锁定GROBID 0.9.0 CRF镜像digest，容器只监听`127.0.0.1:8070`；
- 健康检查12个模型加载、0失败，版本接口返回0.9.0；
- 对真实IS论文PDF执行端到端解析，产生126条全文span、7个章节锚点和126个页码锚点；
- Evidence Matrix与Claim Store正式绑定校验通过，PDF/原始TEI未持久化，路径未输出，题名降级未发生；
- Research Skill Registry真实端到端返回`COMPLETE/FULLTEXT_EVIDENCE_READY`，且Skill结果不含源文件路径；
- 真实服务发现`/api/version`要求JSON，修复客户端406兼容性并增加锁定Compose与运行时审计记录。
- 知识库全量99项、Demo回归26项、Compose配置校验通过。

## Research Skill Layer Phase D-2

- 审计PaperQA2官方commit、`v2026.08.12`标签、Apache-2.0许可证、Python≥3.11约束和默认LLM/metadata依赖；
- 使用Python 3.12.13建立独立运行时，安装PyPI固定版本并冻结77个实际依赖；
- 新增JSON stdin/stdout检索worker，只调用PaperQA2内置`sparse` embedding与`Docs.retrieve_texts`；
- 主进程只把P1 direct allowlist中的摘要/全文span传入worker，并重新验证返回span ID；
- worker环境剥离OpenAI、Semantic Scholar和Crossref等秘密字段，不启用PDF解析、外部论文搜索、LLM、agent或答案生成；
- 输出统一为`EVIDENCE_CANDIDATE_ONLY`，不能直接成为Claim、答案或研究空白；
- GROBID→126条全文span→PaperQA2 5条候选→Claim Store联合验收通过；
- 新增13项D-2测试；知识库全量112项、Demo回归26项通过。

## Research Skill Layer Phase E 与本地界面验收

- Phase E代码落地并复验全量自动回归：知识库115项、Demo回归29项、`app.js`语法检查通过；
- 服务端`/api/proposal`只接受`formal=true`的P3正式空白，并把真实Claim Store、用户约束、蓝图与提纲确认标志传入P4 Provider；新增Skill/Evidence/Saturation/Audit四面板数据；
- 新增`tests/browser_acceptance_harness.py`受控浏览器验收夹具：discovery返回显式标注的夹具论文，synthesis用真实`build_evidence_matrix`+`EvidenceBoundResearchGapProvider`确定性代码生成正式空白与Claim Store，P2→gap_candidates桥接仅存在于夹具；开题Provider保持真实代码并调用本地`fake_deepseek_server`；
- `fake_deepseek_server.py`补受控逐节生成分支（回显section_spec、blueprint_refs、allowed_claim_ids）；修复其分发重构导致的响应发送代码错位；
- API冒烟与浏览器验收均通过四道检查点：空约束被必填与`PROPOSAL_NEEDS_USER_INPUT`拦截→约束回显`USER_CONSTRAINT_CONFIRMATION_REQUIRED`→蓝图+11节提纲+11张任务卡`PROPOSAL_PLAN_CONFIRMATION_REQUIRED`→逐节生成11节`READY_FOR_HUMAN_REVIEW`，Claim/引用/跨节一致性/任务卡四审计全通过；
- 浏览器实测：四步stepper、约束10字段、任务卡与Claim·论文·证据层级绑定、四面板（桌面4列/390px窄屏单列无横向溢出）、「新建研究」重置清空工作流与徽标；
- 发现并修复一处前端偏差：重置后`#proposal-status`残留「待人工复核」与`ready`类，`resetForm`现已复位为「尚未开始」；修复后Demo回归29项、JS语法通过并复跑完整流程验证；
- 界面验收未覆盖：真实P2链路（当前被P1门禁阻断且真实P2不产出空白，P3服务端整合属T05）、浏览器控制台错误计数（本次自动化手段不可读，以全流程无失败请求与无错误UI代证）；
- 全部夹具内容均显式标注「受控测试/界面验收」，不得作为研究结论引用；
- 创建者确认后以`feat/research-workflow-ui`提交（commit `3441a57`，17文件+1216/−90）并推送GitHub：本次连同此前未推送的Research Skill Layer A–D2共11笔本地提交一并发布至`p1-retrieval-quality`分支；提交门禁=敏感信息扫描通过+知识库115项+Demo回归29项+JS语法通过。

## P1精排校准返修（D038，创建者指示补回Q007）

- 方案与D038落盘：`04-分析/01-current/P1精排校准返修/方案.md`；限定确定性杠杆，P0/P1门槛数值不变；
- 基线实验发现c14bd50已把14篇失败压掉13篇但大幅误伤（ai_trust direct层清零）；校准定位为「保住降级+救回误伤」；
- 代码：reranker 2.0.0→2.1.0新增`review_title_signal`（显式排除bibliometric，防误伤人工已标相关的计量论文）；`paper_quality`新增`exclude_scope`与`review_title_demoted`（方向可`reviews_in_scope`豁免）；
- 画像1.1.6→1.2.0（base+overrides版本同步，61方向重编译0错误）：ai_trust补信任×AI对象构念面+`required_any`锚定门+`require_context_match=false`；ai_enabled_is补generative AI短形式与复数语境+metaverse排除；platform_governance补生态治理面；均不点名单篇论文；
- 样本内效果：已标注直接层严格精度0.7667→0.9565，14篇失败压到2篇（均为邻近、Top10尾部、门禁预算内）；3篇相关论文因无摘要按既有保守策略维持manual，不降证据门；
- 验证：新增14项校准专项测试（类型降级、title范围排除、锚定门、回归守护）；知识库全量129项、Demo回归29项通过；权重未动故leave-one-group-out CV分支按方案未触发；
- 产出：`evaluation/p1_rerank_calibration.py`、`calibration_report.json`、`p1_sampled_precision_package_v2.json`、`盲审标注表v2.md`（55篇，16篇从未标注，重叠按组披露）；
- 待办：创建者盲标v2包后按原门槛评分；通过才追加`RERANK_CALIBRATED`。
- 创建者完成v2盲标（46相关/7邻近/1无关/1不确定，55/55行标注合法）。官方评分：总体0.8519≥0.80、六组全≥0.70、明显误召1.85%≤10%——三项精度门禁全过；`all_60_labeled`未过（55/60，两组池浅已在包前披露）。
- 实测补池路径不可行：匿名OpenAlex精确lane 429（RETRIEVAL_PARTIAL），两组新Top-10与已标论文几乎零重叠（需废弃15篇已标、重标约19篇），且按D029认证需0限流。证据归档`04-分析/01-current/P1精排校准返修/refill_*.json`。
- P1门禁判定保持未通过，待创建者定夺：供OpenAlex密钥做干净补池，或确认接受55篇偏差后追加`RERANK_CALIBRATED`并关闭Q007。
- 创建者选择供密钥补池，已在8766实例配置OpenAlex认证额度。两组各跑一次干净全链路：0限流/0失败/0撤稿、reranker 2.1.0；生成v3包60行（13篇待补标：is_communication_media 9、is_cs_hci 4；同组同论文标签沿用v2/v1）。清单`盲审标注表v3.md`；补标后重跑评分，全过才追加`RERANK_CALIBRATED`。
- 创建者补标13篇（全部相关）。v3评分**通过**：总体0.8814≥0.80、六组全≥0.70（0.80/1.0/0.778/1.0/0.80/0.90）、明显误召1.69%、60/60全标注；对比v1总体+0.115、最弱组0.60→0.778。P1追加`RERANK_CALIBRATED`、Q007关闭（D039）；`p1_precision_gate_passed`生产标志切换留待T04/P2由创建者确认，不自动解锁。P1精排校准返修全部收口。

## T04/P2 启动与生产P1解锁

- 创建者确认解锁生产P1标志（D039执行）：`server.py`新增`P1_PRECISION_GATE`（passed=True锚定D039+v3实测+证据路径），synthesis请求携带标志与summary；守护测试验证证据真实且数值一致，证据变更须回退。Demo 30项、KB 129项通过；桩验证flag=True入synthesis。8766已用新代码重启。
- 新增`evaluation/p2_stability_eval.py`：同快照五次运行评测精确一致率/成对ARI/代表论文Jaccard+三门槛；不足语料诚实阻断。修复成功状态误判bug。
- 合成合格快照（32篇/24摘要）通过路径：一致率100%、ARI 1.0、Jaccard 1.0；P0 27篇阻断路径验证。新增6项测试；KB全量135项、Demo 30项通过。
- 待创建者在8766重配OpenAlex密钥后：经真实链路抓D009合格语料快照（≥30篇≥20摘要）→五次稳定性正式证据。
- 创建者重配密钥，经真实解锁链路取语料：D009仅19篇（quota 45仍不足，如实保留缺口）；platform_governance 36篇全摘要（0限流/0失败/0撤稿）→P2首个合格快照（hash 6444f4bf…，携带RERANK_CALIBRATED标记与版本）。
- 真实快照五次稳定性：一致率100%、ARI 1.0、Jaccard 1.0，门槛1–3全过（P0基线0.20/0.5577/0.5347）；5簇每簇≥3、语义连贯。报告与证据落T04验收证据。T04剩余：Athlete/Judge命名、四类评测集、前端质量面板。

## T04收口：Athlete/Judge命名、四类评测集、前端质量面板与浏览器验收

- 命名架构落地：`cluster_naming_provider.py`新增Athlete-Judge三角色（选手A/B独立风格提示词＋盲评Judge，`p2-athlete-judge-naming-2.0.0`）；成员哈希前后校验确保模型不改论文归属，Judge篡改或成员变动即断言失败；模型调用失败回退确定性特征命名；新增arena命名与防篡改测试；
- 诚实降级细化：`stable_subdirection_clustering.py`输出降级/停止原因文本映射、honest_target只降不升、热度五分量（paper_share/recent_growth/citation_velocity/match_score/source_quality）与语言计数（中文0篇缺口显式警示）；
- 四类评测集全过：新增`evaluation/p2_four_set_eval.py`（固定/留出相似/回归/挑战共8用例）——固定集5方向、留出集诚实4方向、P0回归正确阻断、挑战集0簇输出0硬凑，`all_gates_passed=true`，证据`p2_four_set_eval.json`；
- 服务端与前端：画像/检索/评分/聚类/命名五个版本号与数据截止日接入synthesis请求并新增守护测试防回归；前端新增`renderSynthesisQuality`质量面板（语料/稳定性/版本/置信度四卡片＋不足原因区块），方向卡带热度分；
- 浏览器验收全过：新增`tests/p2_browser_acceptance_driver.py`（CDP驱动Edge无头浏览器）＋`tests/p2_browser_acceptance_harness.py`（本机8793运行真实Demo服务，受控夹具论文36/24/14三组显式标注）；四场景×10项检查全过（完整5方向/诚实降级4方向/诚实停止0方向/390px窄屏无横向溢出）；Athlete/Judge受控命名Trace留存（A胜3/B胜2、membership_hash一致、无回退）；唯一控制台报错为既有favicon 404；
- 回归：知识库全量152项、Demo回归33项、JS语法检查通过；
- 文档收口：任务说明（门槛1–6全过）、产出清单、P2当前实现与改造说明、五次稳定性报告后续章节、当前任务与状态第48条均已更新；
- 遗留运行时项：创建者配置DeepSeek密钥后对36篇真实合格快照执行真实模型正式命名并留存Trace（受控验收已证明链路、成员守恒与界面渲染，命名内容不因受控验收而成立）；T04待创建者确认关闭（Q008）。

## T04 真实模型命名收口（含并行会话协作）

- 发现并行会话于16:24-16:27完成T04剩余实现（Athlete/Judge命名器、四类评测集、前端质量面板、Edge无头浏览器四场景验收），本会话验证其全部工作：KB 152项、Demo 33项、四集 all_gates_passed、账本续写无冲突。
- 本会话8766重启加载新代码（旧进程内存为单次调用命名器，其间一次真实命名属旧路径，留作中间产物不作正式证据）。
- 创建者配置双密钥后完成真实模型正式命名：36篇全摘要真实语料（corpus_hash e0e4e348…），arena 三次真实调用，成员哈希守恒，裁判5/5选A留理由，内置自检字节一致；本快照五次稳定性100%/1.0/1.0。证据P2真实模型命名证据.md。
- T04门槛1–6全部双轨（真实+受控）证据通过，待创建者确认关闭。

## T04 归档与 T05 启动（D040）

- 创建者确认关闭T04：六项门槛双轨证据成立（含真实模型正式命名），任务包git mv至`99-done/T04-P2稳定五方向`；Q008关闭（两项待办均完成）。
- T05/P3证据矩阵与研究空白移入`01-current`成为唯一当前任务；AGENTS.md当前入口、状态头部与条目50、D040同步更新。
- T05核心工作：把Research Skill Layer的P3适配器、GROBID全文Sidecar、PaperQA2受控检索接入服务端主链（synthesis→formal gap→Phase E开题），替换D037界定的夹具桥接。

## T05/P3 主链实现与受控验收

- 方案经创建者确认后实施：`gap_candidate_provider.py`（Athlete A/B+盲评Judge，span池强制、内容哈希匿名化、轮转确定性span选取）；synthesis接线（证据矩阵→候选→单次全簇正式化→反证计划）；SynthesisResult扩展formal_gaps/claim_store；subdirection_id贯通UI过滤。
- 四类评测集离线全过（8用例9门禁，伪造span/单篇支持/裁判拒选/虚构反证全阻断）；9项新测试；回归KB 161/Demo 33。
- harness升级：35篇/5组夹具、synthesis不再打补丁（真实主链+受控fake服务器）；浏览器端到端验收通过：5方向5正式空白→Phase E→待人工复核，四面板全绿（P1解锁后首次PASS）。
- 8766已载新代码重启；待创建者配置双密钥做真实模型验收（空白候选与裁判理由需真实成立）。
