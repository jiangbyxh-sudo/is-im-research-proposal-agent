# 2026-08-24 工作日志

## T06 真实重跑v3：三条根因修复验证＋新发现字数系统性欠产

- 创建者重配双密钥后，以`t06_v3_driver.py`复刻v2链路（platform_governance、10zh/45en、同组约束、首个正式gap、innovation_1）真实重跑全链，证据`04-分析/01-current/P1精排校准返修/t06_real_*_v3.json`：
- 检索RETRIEVAL_PARTIAL（36篇，782s）；综合SYNTHESIS_COMPLETE（9个正式gap，Athlete/Judge真实命名214s）；蓝图提纲本地构建；
- **三条根因修复全部验证有效**：①arena正常裁决（`p4-athlete-judge-review-1.2.0`，范围5节=literature_status/theoretical_framework/research_design/feasibility_ethics/research_questions，逐节评分+理由，3节建议修订含具体原因，整体revise(7)）——v2的整场ValueError消失；②timeline无回溯错误（模型遵守"今天2026-08-24之后启动"提示）；③human_subject误报清零（4→0）；
- 状态PROPOSAL_CONTROLLED_PARTIAL，唯一残留门禁失败：`background:words_504_below_624`；
- 根因④（新）：字数欠产是系统性的——提示词只说"六成以上"（模型需自行计算1560×0.6），无字数重试机制；v2 background 611<624、v3 504<624连续两次欠产。

## T06 v4修复：字数下限显式注入＋反馈重试（provider 1.3.0）

- `_generate_section`注入`length_rule`（硬性下限=提纲目标×40%与审计口径同源，MIN_SECTION_WORD_RATIO/MIN_CHECKABLE_TARGET_WORDS从proposal_workflow_controls导入）＋系统提示词改为引用length_rule；
- 调用点新增单次字数重试：首版content低于floor时，以`length_feedback`（含首版长度/floor/扩写指令）重生成一次，取更长版本；重试轨迹（first_length/floor/retried_length/passed/retry_attempts）记入model_audit["length_retry"]，留痕可审计；
- 版本`p4-controlled-proposal-1.3.0`；新增测试`test_under_length_section_is_regenerated_once_with_feedback`（ShortFirstSectionClient首版6字符→重试1168字符达标，调用数12、反馈payload含下限数字、审计含重试轨迹）；回归KB 181/Demo 34全绿；
- 8766已重启加载1.3.0（密钥按设计清空）；v4驱动`t06_v4_driver.py`就绪，待创建者重配双密钥后执行。

## 待办

- 创建者重配双密钥→运行v4驱动（全链~20分钟）→全部门禁通过（READY_FOR_HUMAN_REVIEW）后生成新一轮人工Rubric评分包（门槛3重测）。

## 前端等待超时修复（用户实测"平台治理"方向综合超时触发）

- 现象：创建者以平台治理方向真实检索成功（文献数量达标），但方向综合报"方向综合等待超时"；DeepSeek密钥已配置。
- 根因：前端`app.js`对`/api/synthesize`设120秒AbortController超时，而真实综合含Athlete A/B+Judge三次DeepSeek调用实测需约214秒（v3日志），前端2分钟即放弃——服务器端任务实际仍在正常运行；检索阶段不调用DeepSeek故不受影响。`/api/proposal`（逐节生成11节+arena）与`/api/research`（大数量OpenAlex检索实测13分钟）同样撞120秒墙。
- 修复（`static/app.js`）：综合超时120s→420s（7分钟），开题生成120s→900s（15分钟），检索120s→900s（15分钟）；综合等待初始提示注明"约需3–5分钟，请勿关闭页面"；三条超时文案同步更新。静态文件`no-store`按请求读盘，无需重启服务器（保留已配置密钥），浏览器刷新即生效。
- Node语法检查通过（`tests/check_js_syntax.js`）；服务器端无请求级超时限制（仅OpenAlex 45s/Crossref 6s外部API超时），长请求可正常撑到完成（v3驱动214秒响应为证）。
- 综合job在服务器内存TTL 30分钟（`SYNTHESIS_JOB_TTL_SECONDS`），前端放弃后job_id不可找回，用户需刷新页面重跑检索+综合。
