# P1 精排校准返修 · 结果报告

> 创建者 2026-08-23 确认执行（D038）。方案见同目录 `方案.md`。**最终结论：v3 盲审全部门禁通过，P1 追加 `RERANK_CALIBRATED`，Q007 关闭（D039）。**

## 1. 实施内容

| 层 | 改动 | 版本 |
| --- | --- | --- |
| 代码 | `semantic_reranker.py`：新增 `review_title_signal`（题名级综述/图景/议程信号，显式排除 bibliometric） | `p1-directness-reranker-2.0.0` → `2.1.0` |
| 代码 | `paper_quality.py`：①direct 排除面新增 `exclude_scope`（默认 text，可按方向 title）②`review_title_demoted` 降级（方向可 `reviews_in_scope=true` 豁免） | — |
| 画像 | `direction_profile_overrides.yaml` + base 版本 1.1.6→1.2.0，61 方向重编译 0 错误 | `1.2.0-p1-retrieval` |
| 工具 | `evaluation/p1_rerank_calibration.py`（离线重排+报告+盲审包生成） | — |
| 测试 | `evaluation/test_p1_rerank_calibration.py` 14 项 | — |

### 画像构念级补全（不点名单篇论文）

- `topic_ai_trust`：核心面从单短语 "AI trust" 扩为信任×AI/算法/自动化/机器人等构念变体（含 "trust in artificial intelligence" 自然语序）；direct 增加 `required_any` AI 对象锚定门（信任论文必须出现 AI/算法/自动化等对象，否则最多 adjacent）；`require_context_match=false`（核心短语已编码对象+现象，避免语境短语误杀）；排除面 `trust in information technology`（title 范围）。
- `topic_ai_enabled_information_systems`：核心面补 "generative AI/GenAI" 短形式与 "AI implementation/adoption"；语境面补复数形态；排除面 `metaverse`（title 范围）。
- `platform_governance`：核心面补 "ecosystem governance" 系；语境面补 complementor/platform owner 等平台生态角色词。

`information_behavior` / `topic_decision_support` 沿用 c14bd50 已存在的排除面，未改动。

## 2. 样本内校准效果（60 篇旧标签）

| 指标 | 校准前（1.1.3 认证） | 校准后（1.2.0，样本内） |
| --- | --- | --- |
| 已标注论文直接层严格精度 | 0.7667 | **0.9565** |
| 失败论文仍在 direct 层 | 14/14 | 2/14 |
| 相关论文在 direct 层 | 37/46（扣除 3 篇无摘要人工复核、5 篇未标注） | 44/44 已标注相关（另 3 篇无摘要仍按既有保守策略 manual） |

残留 2 篇失败均为「邻近」且落在 Top-10 尾部（is 组 rank10、is_psychology 组 rank6），未做实例级排除：按组门槛 0.70 预算充足（邻近计宽口径），且实例级排除是上一轮过拟合的直接原因。

**注意：样本内结果乐观**——画像补全基于旧 60 标签，门禁判定只认新一轮独立盲标。

## 3. 验证

- 校准专项测试 14/14；知识库全量 129/129；Demo 回归 29/29。
- 权重未动（focality 0.30 不变），故方案中「仅当 CV 证明增益才调权重」的 CV 分支未触发；所有规则均为构念级、确定性、可判读。
- 61 方向画像 Schema 校验通过；`information_behavior`、`topic_decision_support` 既有排除面回归守护测试通过。

## 4. 第二轮盲审包（待创建者标注）

- `p1_sampled_precision_package_v2.json` + `盲审标注表v2.md`（55 篇）。
- 组内 direct 不足 10 篇的如实披露：`is_communication_media` 7 篇（3 篇已标注相关论文为无摘要 title-only，按既有保守策略走 manual，不为其降低证据门）。
- 与 v1 Top-10 重叠按组披露：is 6、is_cs_hci 7、is_psychology 8、is_communication_media 7、im 5、im_data_ai_knowledge 6；55 篇中含 16 篇从未标注的新论文。
- 评分与门槛不变：总体 ≥0.80、任一分组 ≥0.70、明显假阳性 ≤0.10（复用 `evaluation/p1_sampled_precision.py` 逻辑）。

## 5. 已知边界

- 离线重排只在认证快照已选池内；池外候选不可恢复。若 v2 盲标不达标且怀疑池外遗漏，需创建者提供 OpenAlex 密钥做全漏斗真实重跑。
- 认证快照本身是 1.1.3 时代产物；v2 盲审包验证的是「排序+边界校准」，不是完整 1.2.0 流水线的 61 方向覆盖复测（该复测属后续 P1 发布门禁收口，另行确认是否需要）。

## 6. v3 盲审最终判定（2026-08-23，通过）

- v2 评分：55/60 行，三项精度门禁全过（总体 0.8519、六组全 ≥0.70、误召 1.85%），`all_60_labeled` 未过（两组池浅，包前披露）。
- 创建者提供 OpenAlex 认证额度，两组各跑一次干净全链路（0 限流/0 失败/0 撤稿，reranker 2.1.0）；同组同论文标签沿用（ai_enabled_is 6 篇、social_media 1 篇），v3 包 60 行；创建者补标 13 篇（全部相关）。
- **v3 评分（官方评分器）：总体 Precision@10 = 0.8814 ≥0.80 ✓；六组 = 0.80 / 1.0 / 0.778 / 1.0 / 0.80 / 0.90 全 ≥0.70 ✓；明显误召 = 1.69% ≤10% ✓；60/60 全标注 ✓ → `passed: true`。**
- 对比 v1 认证：总体 0.7667→0.8814（+0.115），最弱分组 0.60→0.778，误召 1.67%→1.69%。
- 处置：T03 历史 `CLOSED BY CREATOR WAIVER / NOT ACCEPTED` 不改写；P1 现状态 `NOT_ACCEPTED → RERANK_CALIBRATED (2026-08-23, v3盲审)`；Q007 关闭。服务端 `p1_precision_gate_passed` 生产标志切换属 T04/P2 范围，须创建者另行确认（D039）。
- 证据链：`p1_sampled_precision_package_v3.json` / `_v3_reviewed.json` / `p1_sampled_precision_result.json`（评分器输出） / `refill_auth_*.json`（两组认证重跑响应）。
