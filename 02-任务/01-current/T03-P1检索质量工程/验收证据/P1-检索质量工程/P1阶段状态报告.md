# P1 检索与排序质量工程阶段状态报告

日期：2026-08-22  
分支：`p1-retrieval-quality`  
结论：**P1 已进入实施，但尚未通过验收；不得进入 P2，也不得声称检索质量问题已经修复。**

## 已完成的可验证实现

- 61/61 个版本化双语 `DirectionProfile`；每个方向均有至少 3 个近义表达、纳入/排除边界、Provider 查询、来源路由、期刊池、文献类型与至少 2 条回退查询。
- OpenAlex 主发现与 Crossref 白名单回退；Semantic Scholar、DBLP 可选连接器；CORE、Unpaywall和授权库诚实标记为未配置。
- Provider-neutral `PaperRecord P1`、字段级 provenance、保守去重、撤稿/文献类型/期刊池/年限硬闸门。
- 固定权重 `MetadataScore` 与完整分项解释；语义、影响与阈值均标记为未校准。
- 429、零结果、语言缺口、过严闸门和过窄查询的结构化诊断；回退查询日志与 15 分钟进程内缓存。
- 前端分页面展示候选漏斗、来源状态、自动扩展、证据层级、Provider 和评分版本。
- OpenAlex API key 可在设置页仅写入当前服务进程；不写文件、不回显。

## 认证额度下的61方向完整流水线

当前机器可读汇总：`p1_full_pipeline_coverage.json`

当前报告：`P1完整流水线61方向覆盖报告.md`

逐方向完整API响应Trace：`traces/full_pipeline/*.json`（61个）

历史匿名主路由预检仍保留在 `p1_primary_route_coverage.json` 和 `traces/coverage/`，但不再作为当前覆盖结论。

本轮通过8766实例的正式 `/api/research` 路径运行，设置 `run_synthesis=false`，因此覆盖OpenAlex查询扩展、统一记录、去重、硬闸门、MetadataScore、Crossref回退、语言配额和零结果诊断；没有进入P2聚类或开题生成。

| 指标 | 当前实测 | P1门槛 | 判定 |
|---|---:|---:|---|
| DirectionProfile覆盖 | 61/61 | 61/61 | 通过 |
| 原始候选≥20 | 61/61（100%） | 至少58/61（95%） | 通过 |
| ≥70分候选≥10 | 1/61（1.6%） | 至少55/61（90%） | 未通过 |
| Provider/Runner失败 | 0/61 | 0 | 通过本轮检查 |
| 发生限流的方向 | 0/61 | 不得误报为零结果 | 认证额度阻塞已消除 |
| 最终撤稿记录 | 0 | 0 | 通过本轮检查 |
| Precision@20 | 未测 | ≥0.80 | 未通过；必须人工盲标 |

本轮共记录18,241条原始候选、1,633条硬闸门通过记录、610条55–69分边界候选和106条70分以上候选。原始候选中位数为321，硬闸门通过数中位数为29，但70分以上候选中位数仅1。44/61方向返回部分结果，17/61没有任何记录越过70分阈值；61/61方向均存在中英文配额缺口，最终106条入选记录全部为英文。

聚合排除记录显示：

- `source_not_qualified`：11,874条；
- `wrong_document_type`：1,631条；
- `missing_source_title`：711条；
- `retraction_or_correction`：32条，最终入选为0；
- `metadata_score_below_55`：排除日志中仅52条；
- `missing_title`：2条。

第一处新的关键偏差是：认证已解决原始召回，但OpenAlex宽检索返回的大量论文不属于方向级合格来源；通过硬闸门后，未校准MetadataScore又把多数记录留在70分以下。不能通过删除期刊范围闸门来补数，应先校准方向查询、方向级期刊池和评分。

本轮还发现一处Trace完整性缺口：1,633条硬闸门通过记录中，只有106条达到70分、610条处于55–69分，理论上有917条低于55分；Crossref合并后的实现没有把这些记录全部写入排除日志，因此日志只显示52条 `metadata_score_below_55`。这不会改变本轮覆盖计数，但必须在人工校准前修复。

## 端到端页面核验

- 独立测试实例：`http://127.0.0.1:8766/`；没有替换仍保存 DeepSeek 进程配置的 8765 实例。
- 目录加载：62 个 `<option>`，即 1 个提示项 + 61 个知识库方向。
- 设置页和健康接口：正确显示OpenAlex“已认证”；健康响应只返回布尔状态，不回显密钥。
- 认证探针：`AI-enabled information systems` 返回322条原始候选、29条硬闸门通过记录和3条70分候选；耗时19.09秒，没有429。
- 完整61方向运行中没有OpenAlex限流或API/Runner失败；未生成模拟论文。
- 浏览器控制台错误：0。

## 自动测试

- Demo 回归：21/21 通过。
- P1 单元/挑战：6/6 通过，覆盖 61 画像、去重与 provenance、撤稿/错误来源硬闸门、固定评分公式与未校准标识、缓存命中、429 不误报为空结果。
- 新增 `p1_full_pipeline_scan.py`：只通过localhost API使用已配置服务，不读取、接收、输出或保存OpenAlex密钥；逐方向即时保存Trace并生成汇总报告。
- JavaScript 语法检查、Python 编译检查通过。

## 未完成项与下一门禁

1. 修复Crossref合并后低于55分候选未完整进入排除日志的Trace缺口。
2. 基于逐方向Trace调整查询与方向级期刊池；不得通过删除 `source_not_qualified` 闸门补数。
3. 建立人工盲标集并计算Precision@20；随后校准55/70/85阈值、语义相关分与年份-领域归一影响分。
4. 实现并验证Athlete A/B与盲评Judge，之后才可启用最终 `PaperMatchScore`。
5. 重跑回退、撤稿、重复、仅题名、中文缺口与Provider超时挑战集，再次执行61方向完整流水线并完成P1总验收。

在上述门禁通过前，P1 状态保持 **IN PROGRESS / NOT ACCEPTED**。
