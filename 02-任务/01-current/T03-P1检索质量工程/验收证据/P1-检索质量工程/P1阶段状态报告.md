# P1 检索与排序质量工程阶段状态报告

日期：2026-08-22  
分支：`p1-retrieval-quality`  
结论：**P1 已进入实施，但尚未通过验收；不得进入 P2，也不得声称检索质量问题已经修复。**

## 已完成的可验证实现

- 61/61 个版本化双语 `DirectionProfile`；每个方向均有至少 3 个近义表达、纳入/排除边界、Provider 查询、来源路由、期刊池、文献类型与至少 2 条回退查询。
- OpenAlex 主发现与 Crossref 白名单回退；Semantic Scholar、DBLP 可选连接器；CORE、Unpaywall和授权库诚实标记为未配置。
- Provider-neutral `PaperRecord P1`、字段级 provenance、保守去重、撤稿/文献类型/期刊池/年限硬闸门。
- 固定权重 `MetadataScore` 与完整分项解释；语义、影响与阈值均标记为未校准。
- 429、零结果、语言缺口、过严闸门和过窄查询的结构化诊断；回退查询日志与 15 分钟进程内缓存。
- 前端分页面展示候选漏斗、来源状态、自动扩展、证据层级、Provider 和评分版本。
- OpenAlex API key 可在设置页仅写入当前服务进程；不写文件、不回显。

## 真实 61 方向主路由预检

原始数据：`p1_primary_route_coverage.json`  
逐方向 Trace：`traces/coverage/*.json`（61 个）

| 指标 | 当前实测 | P1 门槛 | 判定 |
|---|---:|---:|---|
| DirectionProfile 覆盖 | 61/61 | 61/61 | 通过 |
| 原始候选 ≥20 | 33/61（54.1%） | ≥95% | 未通过；28 个方向被 HTTP 429 中断 |
| ≥70 分候选 ≥10 | 0/61 | ≥90% | 未通过 |
| Precision@20 | 未测 | ≥0.80 | 未通过；必须人工盲标 |
| 最终撤稿记录 | 单元/挑战测试为 0；61 方向完整流水线未重跑 | 0 | 尚不能判定完整验收通过 |

预检在匿名额度下运行。前 33 个方向均返回 100 条原始候选；后 28 个方向统一为 `HTTPError/429`，因此不能把它们解释为研究方向零结果。成功返回的候选中，主要排除原因为：

- `source_not_qualified`：3177 条；
- `wrong_document_type`：447 条；
- `missing_source_title`：197 条；
- `retraction_or_correction`：9 条，均未进入候选；
- `missing_title`：1 条。

成功方向的硬闸门通过数最高为平台治理 11 条，但未校准 70 分阈值下仅 3 条候选；这说明下一步需要校准方向查询、方向级期刊池和评分，而不是放宽期刊质量边界。

## 端到端页面核验

- 独立测试实例：`http://127.0.0.1:8766/`；没有替换仍保存 DeepSeek 进程配置的 8765 实例。
- 目录加载：62 个 `<option>`，即 1 个提示项 + 61 个知识库方向。
- 设置页：正确显示 OpenAlex“匿名额度”与进程内密钥入口。
- 平台治理、中文 1 / 英文 1 的匿名限流场景：OpenAlex 429 后 Crossref 返回 19 条白名单记录；19 条通过硬闸门，0 条达到 70 分，5 条为边界候选；页面显示 `rate_limited`、`quality_gate_too_strict`、`language_coverage_gap`，未生成模拟论文。
- 浏览器控制台错误：0。

## 自动测试

- Demo 回归：21/21 通过。
- P1 单元/挑战：6/6 通过，覆盖 61 画像、去重与 provenance、撤稿/错误来源硬闸门、固定评分公式与未校准标识、缓存命中、429 不误报为空结果。
- JavaScript 语法检查、Python 编译检查通过。

## 未完成项与下一门禁

1. 配置 OpenAlex API key 后重跑 61 方向完整流水线，消除匿名额度造成的 28 个不可判定方向。
2. 基于逐方向 Trace 调整查询与方向级期刊池；不得通过删除 `source_not_qualified` 闸门补数。
3. 建立人工盲标集并计算 Precision@20；随后校准 70 分阈值、语义相关分与年份-领域归一影响分。
4. 实现并验证 Athlete A/B 与盲评 Judge，之后才可启用最终 `PaperMatchScore`。
5. 重跑回退、撤稿、重复、仅题名、中文缺口与 Provider 超时挑战集，并完成 P1 总验收。

在上述门禁通过前，P1 状态保持 **IN PROGRESS / NOT ACCEPTED**。
