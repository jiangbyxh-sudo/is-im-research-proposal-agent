# P1精确召回返修阶段报告

> 状态：P1.1–P1.8代码首轮完成；认证61方向复测与P1.9人工抽检未完成。本文不是P1通过证书。

## 已完成

- P1.1：OpenAlex、Crossref及后续来源统一进入 `evaluate_candidate`；每篇去重候选只有 `gate_reject / rank_reject / boundary / eligible / manual_review` 一个终态；新增候选Trace和计数守恒断言。
- P1.2：画像改为 `direction_profile_base.yaml + direction_profile_overrides.yaml + Topic/Source注册表` 编译；61/61通过新Schema。66本期刊取得无歧义OpenAlex Source ID，25本保持 `pending_review`；43/61方向取得Topic候选，18/61因公共额度429记录为解析缺口，所有Topic候选均保持待审核，未自动采用第一条。
- P1.3：新增可重放 `OpenAlexQueryPlan`；日期、文献类型、已审核Topic/Source、语言和摘要要求可进入Provider filter；实现cursor分页、逐页遥测、Lane漏斗和基于边界后候选数的停止条件。
- P1.4：Crossref改为返回raw records，最终资格统一由候选账本判断；查询顺序按Tier A、Tier B、Adjacent排列；Semantic Scholar/DBLP继续保持可选且未配置时不冒充已查询。
- P1.5：执行核心facet、必要情境和负面边界；来源质量、方向适配与内容相关性分开保存；Unknown来源进入人工复核。
- P1.6：移除固定70分准入逻辑，使用Topic路线、透明的多语种加权重合、facet覆盖、来源适配、证据、时效和归一影响组成混合精排；没有embedding时明确标注为非embedding实现。
- P1.7：保留Top-N Judge替换口，当前未启用；它不是P1主检索依赖。
- P1.8：新增中文独立Lane、CJK n-gram、中文Source ID、中文覆盖报告和英文不得补中文配额的强制检查。
- 前端：候选漏斗改为“进入精排/边界淘汰/人工复核/Trace守恒”，展示Lane和新的精排版本。

## 未完成

- 新链路尚未在认证额度下完成61方向全量复测；服务已重启加载新代码，OpenAlex进程内密钥需重新配置。
- 18个方向的Topic候选解析被公共额度429阻断；43个方向的Topic候选仍需基于描述和样例论文审核后才能进入正式Topic filter。
- 25本期刊Source解析仍为待复核。
- P1.9六分组Top10共60篇人工抽检尚未生成最终标签与评分；因此不能给出Sampled Precision@10、明显误召率或P1通过结论。
- 未达到Hybrid门槛前不启用可选Athlete/Judge；未进入P2、P3、P4。

## 第一次关键偏差

旧链路把宽泛分组上下文加入普通同义词，并将整个分组期刊池视为同等相关；OpenAlex只做字符串搜索，边界未执行，导致临床AI、普通HCI、纯算法等邻近研究大量进入候选。Crossref合并后又复制了一套分类分支，造成低分记录漏Trace。

## 本次只改变了什么变量

只改变论文发现、画像、结构化查询、候选账本、边界、来源分层、精排和检索解释；没有修改五方向聚类、研究空白综合、开题报告或写作指导Prompt。

## 查询Lane变化

```text
A：已审核Topic + Tier A来源
B：方向精确短语 + Tier A/B来源
C：Topic或facet扩大召回
D：可选Seed扩展
E：中文精确/召回词 + 中文Source ID
```

Topic尚未审核时A/C的Topic过滤不会启用；B和E仍执行已审核Source过滤。停止条件由原始候选数改为边界后中英文候选目标。

## 候选漏斗变化

旧认证基线为18,241条原始候选、1,633条硬闸门通过、610条55–69分、106条70分以上。新链路不再沿用55/70/85固定阈值；新漏斗必须在认证61方向复测后重新报告，本阶段不拿单元测试数据冒充实网变化。

## 固定回归样本结果

- 临床AI-only论文：不进入 `eligible`，通过。
- GenAI与IS研究议程论文：进入 `eligible`，通过。
- HCI-only论文：不进入 `eligible`，通过。

## Trace守恒结果

- 单元测试验证去重候选数等于五个互斥终态之和；Crossref低相关记录仍保留候选Trace。
- 61/61真实方向守恒尚待认证复测，不能提前写为通过。

## 自动测试结果

- P1专项：24/24通过。
- Demo回归：21/21通过。
- 61个DirectionProfile编译校验：61/61通过。
- 外部知识库结构校验：通过。
- 本地知识库checksum将在本批文件稳定后统一重建并复验。

## 风险与限制

- OpenAlex公共额度已出现429；最终实测必须使用设置页中的进程内认证额度。
- Topic候选未审核时，系统依赖Source路线与facet边界，覆盖和精度是否达标只能由真实扫描与人评回答。
- 无CNKI/万方授权，中文固定数量不能承诺；中文不足将独立报告并使整体P1保持PARTIAL/NOT_ACCEPTED。
- 当前多语种相似度是透明的确定性加权重合，不是embedding或cross-encoder；若人评不达标再替换后端。

## 下一步

1. 在8766设置页重新配置OpenAlex API key。
2. 运行61方向认证全链路扫描，确认61/61 Trace守恒并比较边界后候选漏斗。
3. 补齐/审核Topic与Source歧义项，按异常方向迭代画像。
4. 生成六分组60篇人工抽检包并计算Sampled Precision@10。
5. 只有全部技术、覆盖、精度和中文门禁满足后，才可把P1标记为通过并进入P2。

