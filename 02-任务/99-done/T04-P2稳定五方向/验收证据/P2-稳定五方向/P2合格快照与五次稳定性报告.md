# P2 合格语料快照与五次稳定性报告（T04 首份验收证据）

日期：2026-08-23
状态：**T04验收门槛1–6全部通过**。门槛1–3见下表；门槛4语料条件满足；门槛5诚实停止行为由四类评测集与浏览器验收覆盖；门槛6四类评测集Trace见`p2_four_set_eval.json`，Athlete/Judge命名与前端面板见`BrowserAcceptance/`。

## 1. 语料快照（真实、经解锁生产链路获取）

| 项 | 值 |
| --- | --- |
| 方向 | `platform_governance`（平台治理） |
| 获取方式 | 8766实例 `/api/research` 真实检索（生产链路，P1标志解锁后首跑） |
| 数据截止日 | 2026-08-23 |
| 画像 / 检索 / 评分版本 | `1.2.0-p1-retrieval` / `p1-query-plan-1.0.0` / `p1-directness-reranker-2.1.0` |
| P1风险标记 | `RERANK_CALIBRATED`（总体0.8814、最弱组0.778、误召1.69%、60行，D039） |
| direct-eligible论文 | 36篇（全部带摘要） |
| 限流 / Provider失败 / 撤稿 | 0 / 0 / 0 |
| `corpus_hash` | `6444f4bf630e205a1e425e60fbfdfa38a53026c4f77e78feb8896084743ec198` |
| 充分性判定 | `P2_CORPUS_READY`（reasons=[]） |

选择说明：固定主案例 D009（`topic_ai_enabled_information_systems`）同日真实检索仅19篇direct-eligible（英文quota提至45仍不足，缺口如实保留），低于30篇门槛，不得用于五方向样例；改用认证时代eligible=36的 `platform_governance` 作为P2首个合格快照。响应原文已归档（`live_d009_corpus_response.json`、`live_pg_corpus_response.json`）。

## 2. 五次稳定性（同一冻结快照，`p2-tfidf-average-linkage-2.0.0`）

| 指标 | P2门槛 | 实测 | 判定 |
| --- | --- | --- | --- |
| 精确输出一致率（同缓存键） | 100% | **100%**（5/5次逐字节一致） | 通过 |
| 成对ARI（10对） | ≥0.85 | **1.0** | 通过 |
| 代表论文Jaccard（10对） | ≥0.80 | **1.0** | 通过 |
| 五次运行状态 | — | 全部`P2_CLUSTERING_COMPLETE` | — |

对比P0模型自由归簇基线：精确一致率0.20→1.00、ARI 0.5577→1.0、Jaccard 0.5347→1.0。确定性算法下逐次差异为工程缺陷而非统计波动，本结果由代码确定性保证并有测试守护。

## 3. 聚类组成（36篇→5簇，每簇≥3）

| 簇 | 篇数 | 代表特征词 |
| --- | --- | --- |
| cluster_001 | 19 | platform, complementors, governance, digital |
| cluster_002 | 6 | data exchange, principles, design, platform |
| cluster_003 | 5 | government, regulation, development, governance |
| cluster_004 | 3 | algorithmic, stakeholder, transparency, coordination |
| cluster_005 | 3 | orchestration, regulatory, externalities, evolution |

簇语义经人工目检连贯（平台生态治理/数据交换/政府监管/算法利益相关者/监管演化）；正式命名按T04计划由Athlete A/B+Judge在后续步骤完成，本报告不代命名。

## 4. 诚实停止与不足行为（门槛5行为面）

- P0固定27篇快照（0摘要）→ `P2_BLOCKED_BY_P1_PRECISION`/不足，不生成聚类（`p2_p0_stability.json`）。
- D009当日19篇 → 低于30篇门槛，未生成五方向样例（响应归档）。
- 合成不足语料单测守护：`paper_count_below_30` 阻断且无聚类输出。
- 硬凑五方向次数：0。

## 5. T04收口补充证据（同日完成）

- Athlete A/B独立命名固定簇 + Judge盲评选择：已实现并经受控浏览器验收验证链路与成员哈希守恒（`BrowserAcceptance/p2_arena_naming_trace.json`，A胜3/B胜2，`membership_invariant: true`）；真实DeepSeek对36篇真实快照的正式命名待创建者配置密钥后执行；
- 固定集、留出相似集、回归集与挑战集四类评测与Trace：`p2_four_set_eval.json`（8用例全过，含留出集诚实4方向、P0回归集阻断、四类挑战集诚实停止，硬凑五方向违规0次）；
- 前端质量信息面板与浏览器验收：`BrowserAcceptance/浏览器验收记录.md`（Edge无头浏览器四场景10项检查全过：完整/降级/停止/390px窄屏无溢出；语料量、数据截止日、五个版本号、确定性自检、热度区间、命名来源、P1门禁状态、不足原因全部真实渲染）；
- 中文0篇缺口在面板以warn显式呈现（"中文 0 篇（缺口，不用英文补数）"），未用英文补数。

## 6. 证据文件

- `p2_qualified_corpus_snapshot.json`（冻结快照，含全部36篇canonical论文与corpus_hash）
- `p2_qualified_stability.json`（五次稳定性评分器输出）
- `../live响应：04-分析/01-current/P1精排校准返修/live_pg_corpus_response.json`、`live_d009_corpus_response.json`
- 评测工具：`11_implementation/evaluation/p2_stability_eval.py`（含6项单测）
