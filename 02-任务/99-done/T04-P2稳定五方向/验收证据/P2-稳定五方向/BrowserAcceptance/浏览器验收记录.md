# P2 前端质量面板浏览器验收记录（T04 验收证据）

日期：2026-08-23
结论：**全部通过**（4场景×10项检查全绿；唯一控制台报错为既有favicon.ico 404，先于P2存在，不计入失败）。

## 1. 验收方法

- 驱动方式：Edge无头浏览器（Chromium，CDP协议），真实浏览器渲染与布局；
- 服务端：`tests/p2_browser_acceptance_harness.py`在本机8793端口运行真实Demo服务；
- 论文来源：受控夹具（36/24/14篇三组，全部显式标注"Controlled fixture / UI acceptance only"，不代表真实检索结果）；
- P2链路为**真实生产代码**：语料快照→确定性TF-IDF平均链接聚类→诚实降级门禁→热度计算→Athlete A/B+盲评Judge命名（经本地`fake_deepseek_server`返回受控命名响应）→`synthesis_audit`透传前端→`renderSynthesisQuality`渲染；
- 驱动脚本：`tests/p2_browser_acceptance_driver.py`（可重复执行，退出码0=全过）。

## 2. 四个场景与检查项

| 场景 | 方向 | 语料 | 预期 | 结果 |
| --- | --- | --- | --- | --- |
| 完整路径（桌面1280px） | platform_governance | 36篇/36摘要 | 5方向+完整面板 | 10/10通过 |
| 诚实降级（桌面） | topic_data_governance | 24篇/24摘要 | 4方向+降级警告+原因 | 10/10通过 |
| 诚实停止（桌面） | topic_it_governance | 14篇/14摘要 | 0方向+停止原因 | 10/10通过 |
| 窄屏复检（390px） | platform_governance | 36篇/36摘要 | 5方向+无横向溢出 | 10/10通过 |

每场景10项检查：面板可见、面板在激活视图真实渲染（`offsetParent`非空）、方向数符合诚实目标、面板标题、语料计数（输入/direct/带摘要/语言分布含"中文0篇缺口"警示）、五个版本号（画像1.2.0-p1-retrieval/检索p1-query-plan-1.0.0/评分p1-directness-reranker-2.1.0/聚类p2-tfidf-average-linkage-2.1.0/命名p2-athlete-judge-naming-2.0.0）、置信度（方向结论/热度区间/Athlete A/B+盲评Judge/P1门禁已解锁）、方向结论文案、降级原因展示、无横向溢出。

关键面板文案实测：完整路径显示"五方向完整（5 个方向）"；降级路径显示"诚实降级：证据支撑 4 个方向"（warn色调）+"direct论文不足30篇，无法支撑五个方向"；停止路径显示"证据不足：已诚实停止，未生成方向"+四条停止原因。中文0篇缺口以warn显式呈现（"中文 0 篇（缺口，不用英文补数）"）。

## 3. Athlete/Judge 盲评Trace（受控命名）

`p2_arena_naming_trace.json`：provider=athlete_judge_arena，5簇winners={cluster_001:athlete_a, cluster_002:athlete_b, cluster_003:athlete_a, cluster_004:athlete_b, cluster_005:athlete_a}（A胜3/B胜2，两种胜出路径均渲染）；membership_hash前后一致（模型未改动论文归属）；无命名回退。命名内容均为受控夹具文本（"受控测试簇名A/B"），仅证明链路与界面，不构成真实方向命名结论。

## 4. 证据文件

| 文件 | 内容 |
| --- | --- |
| `p2_browser_acceptance_report.json` | 四场景检查结果、面板实测文本、控制台错误、判定 |
| `p2_complete_desktop.png` | 完整路径截图（方向质量信息面板+5方向卡） |
| `p2_degraded_desktop.png` | 诚实降级截图（4方向+warn+原因） |
| `p2_stopped_desktop.png` | 诚实停止截图（0方向+原因清单） |
| `p2_complete_narrow_390.png` | 390px窄屏截图（无横向溢出） |
| `p2_arena_naming_trace.json` | 完整路径API响应（含naming audit全量Trace） |

## 5. 边界声明

- 夹具命名仅验证架构与界面，**真实DeepSeek对36篇真实合格快照的正式命名**待创建者在检索设置中配置DeepSeek密钥后经真实链路执行（与GROBID/OpenAlex运行时验收同模式：先完成代码与受控验收，运行时验收待密钥）；
- favicon.ico 404为既有问题（static目录无该文件且HTML未引用），如实记录，未在本次修复；
- 本验收不改变P1以D030带风险关闭、P3未启动的状态。
