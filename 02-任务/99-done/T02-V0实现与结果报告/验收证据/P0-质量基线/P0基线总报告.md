# P0 · 真实质量基线总报告

> **总判断：P0 评测基线材料完整，产品质量未通过。三个用户 Bad Case 均未被声明为已修复，本阶段没有进行开题 Prompt 优化。**

## 已完成

- 61/61 个方向实网覆盖运行及逐方向 Trace；
- 同一 27 篇固定快照连续五次稳定性运行；
- 当前开题生成器的一次真实样本与人工 Rubric；
- 写作指导项目针对性人工 Rubric；
- 7 个挑战题与成功、恢复、阻断三类关键 Trace；
- Crossref 限流安全修正：public pool 并发≤1、polite pool≤3、读取限流头并按 429 退避；
- 状态文档明确“受控结构验收不等于实网质量验收”；
- 20 项自动化回归测试通过；
- 闭锁校验器返回 `P0_BASELINE_COMPLETE`。

## 未完成

- 61 方向双语查询画像、多信源检索、自动扩检与论文级语义评分；
- 确定性聚类、缓存键、第二运动员与裁判；
- 摘要/全文证据矩阵与正式研究空白门禁；
- 分节开题、claim audit、跨节一致性矩阵与项目专属指导；
- 留出相似集、滚动集、双人标注和人评校准；
- 货币成本核算。

## Checkpoint

| Checkpoint | 结果 | 说明 |
|---|---|---|
| P0 材料完整性 | PASS | 61 覆盖、5 次稳定性、2 份人工评分、74 份 Trace 均存在 |
| 覆盖质量 | FAIL | 15/61 零结果，原始候选和合格候选覆盖远低于目标 |
| 稳定五方向 | FAIL | ARI 0.5577、Jaccard 0.5347、精确一致率 0.20 |
| 开题总 Checkpoint | FAIL | 题名级证据推断内容，且 RQ/理论/方法不一致 |
| 指导项目针对性 | FAIL | 缺项目内容、阻塞项、骨架和下一门槛 |
| 撤稿护栏 | PASS（挑战题） | 撤稿记录未进入正式结果 |
| 证据不足阻断 | PASS（挑战题） | 4 篇输入时未调用模型、未硬凑方向 |

## Rubric

- 开题人工 Rubric：**36.25/100，BLOCKED**；目标 ≥85 且核心维度≥70、Checkpoint 全过。
- 写作指导项目针对性：**16.5/100，BLOCKED_GENERIC_GUIDANCE**。
- 当前为单评审者修复前基线，尚未进行导师/方法教师校准。

## 已知失败集结果

| Bad Case | 基线结果 | 判断 |
|---|---|---|
| 重复检索无法稳定得到五方向 | 5 次均给 5 个方向，但精确一致率0.20，空白数9/5/10/3/7 | 已复现，未修复 |
| 开题和指导不可用 | 开题36.25，指导16.5，Checkpoint失败 | 已复现，未修复 |
| 61方向很多返回0篇 | 15/61为0，零结果率24.6% | 已复现，未修复 |

覆盖细项：36/61（59.0%）方向获得至少 20 篇原始候选，目标≥95%；39/61（63.9%）获得至少 10 篇去重候选，目标≥90%；中文达到10篇为22/61，英文达到20篇为28/61。当前烟测预算为每种语言最多2本期刊、每刊20条，结果是基线而非生产上限。

## 留出相似集结果

**未建立、未运行。** P0 只建立固定 Bad Case 与挑战集；在没有留出集前，任何后续改动都存在对固定样本过拟合的风险。

## 回归集结果

20/20 自动化测试通过，覆盖 API 状态流转、目录、密钥不回显、Crossref 限流和基础结构。它们证明接口及护栏没有回归，**不能证明实网覆盖、稳定性或开题质量通过**。

## 挑战集结果

7 题中 6 题通过、1 题失败：

- 通过：429 恢复、终止性超时可见且不伪造、中文缺口不跨语言填充、重复 DOI 去重、撤稿排除、证据不足阻断；
- 失败：无摘要论文没有显式暴露 `evidence_level=title_only`。

## 第一处关键偏差

第一次改变正确路径的偏差发生在检索计划阶段，而不是开题 Prompt：61 个方向没有逐方向的双语查询画像；服务把目录 `label` 直接作为 Crossref 查询词，目录中的 alias 没有用于扩检，期刊路由也只按粗粒度分组。这使大量方向在模型调用前就发生召回不足或相关性门禁清零。

## 延迟与成本

- 61 方向覆盖：平均 9,339.95 ms，p50 8,719 ms，p95 14,625 ms；
- 五次综合：合计 156,296 ms、26,997 Token；
- 开题生成：52,813 ms、8,455 Token；
- 模型基线合计：209,109 ms、35,452 Token；
- 货币成本：`NULL/无法核算`，原因是提供方价格表未配置。

## 完整 Trace

- 总计 74 份：61 覆盖、5 稳定性、7 挑战、1 开题；
- 成功检索：`traces/coverage/FIX-COVERAGE-001.json`；
- Provider 429 恢复：`traces/challenges/CHALLENGE-CROSSREF-429.json`；
- Provider 终止性超时：`traces/challenges/CHALLENGE-PROVIDER-TIMEOUT.json`；
- 证据不足阻断：`traces/challenges/CHALLENGE-INSUFFICIENT-EVIDENCE.json`；
- 开题完整输出：`traces/proposal/FIX-PROPOSAL-001.json`。

## 风险与限制

- Crossref 是当前唯一实网论文信源；覆盖结果受单信源元数据和期刊 ISSN 路由限制；
- 固定语料没有摘要/全文，无法验证研究结论或正式空白；
- 稳定性只是单一方向、单一快照的五次基线；
- 人工评分仅一名评审者，未做标注者一致性；
- 当前报告没有修复前后对比，因为 P1–P4 尚未实施；
- API 密钥只在当前服务进程内存中，服务重启后需要重新配置。

## 下一步

P0 至此只完成“测量系统与修复前基线”。下一阶段应从第一次关键偏差进入 P1：先建立 61 方向版本化双语查询画像与多信源 PaperRecord，再做去重、门禁、可解释评分和自动扩检；在覆盖门槛达到前，不进入稳定聚类或开题 Prompt 改造。
