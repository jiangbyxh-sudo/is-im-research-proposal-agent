# T08计划产出清单

- 数据模型拆分与迁移代码（DirectionProfile / RetrievalPolicy / SourcePolicy）；
- feature flag `PROPOSAL_RETRIEVAL_POLICY_V2=1` 与旧逻辑可运行性保障；
- Query Plan 重写（七类Lane与source_mode）、query_expansion.py、adaptive_retrieval_controller.py；
- 候选资格重写（candidate_ledger / paper_quality）与三层Corpus（Discovery / Evidence / HighConfidence）；
- abstract_enrichment.py、corpus_readiness.py、聚类与空白生成的readiness联动改造；
- server.py 新参数与响应字段（corpus_readiness、coverage_by_lane、source_gate_audit、retrieval_rounds等）；
- 61方向baseline（`04-分析/01-current/retrieval_v2_baseline/`，JSON+CSV）；
- V0–V5消融实验（retrieval_ablation.py，逐方向JSON+汇总CSV+分位数+各维度贡献归因）；
- 每方向retrieval audit；
- 单元测试与集成测试（计划第十五节20项）；
- README/架构文档更新、CHANGELOG、已知限制列表、回滚说明、最终总结（根因/修改/前后对比/未解决/推荐默认配置）。

状态：全部未开始。
